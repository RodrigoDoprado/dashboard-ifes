
export interface TeacherInterface{
    id?: string,
    enroll?: string, // matricula
    firstName?: string,
    lastName?:string,
    avatar?: string,
    subjects?: any //materias,
    created_at?: Date
}